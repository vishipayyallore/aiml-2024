# This is a Node JS API server that is used to interact with the Azure OAI API

## Few Commands

```bash
npm init -y

npm install express dotenv cors @azure/openai

npm install --save-dev nodemon
```
